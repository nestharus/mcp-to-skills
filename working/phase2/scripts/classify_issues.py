"""Legacy rule-based classifier (disabled).

This module intentionally contains no runtime logic now that
classification and issue_type decisions are sourced exclusively
from Droid batch outputs.
"""
