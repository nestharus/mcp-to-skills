# STYLE issues plan for docs/to_integrate/STYLE_5.md

This plan is generated from `issues_index.csv` and is scoped to `docs/to_integrate/STYLE_5.md`.

## Relevant issues

| id | doc | line_no | description_text | issue_type | classification | classification_ref | notes |
|---|---|---|---|---|---|---|---|
| ISS-0105 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 43 | **Project structure mismatch**: The sample project layout (`api/routes/users.py`) does not match this repository, which uses `app/routes/metadata_router_v1.py` and MCP-focused services; examples should be updated to use the actual module structure. | Staleness | CONFLICT | Code Standards & Architecture | STYLE_5’s example layout (`api/routes/users.py`) is outdated relative to the current `app/routes/metadata_router_v1.py`/MCP structure; Migration §2 only says STYLE_5 will be merged into `docs/code-style-guide.md`, so examples must be manually rewritten to match the real project topology. |
| ISS-0106 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 44 | **Service layer not integrated**: STYLE_5 assumes thin route handlers delegating to services, but `app/routes/metadata_router_v1.py` currently contains hardcoded logic and does not call `app/services/mcp_manager.py`, so the pattern is aspirational. | Conflicts | CONFLICT | Code Standards & Architecture | STYLE_5 describes thin handlers delegating to services, but current code in `app/routes/metadata_router_v1.py` and `app/services/mcp_manager.py` does not yet follow that pattern; Migration §3 tracks code/config alignment separately, so this aspirational mismatch must be resolved by future refactors coordinated with the architecture/style docs. |
| ISS-0107 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 45 | **Overlap with STYLE_6**: Function design and route handler patterns are repeated in STYLE_6's router sections; these should be consolidated into a single architecture/style document. | Duplicates | CONFLICT | Code Standards & Architecture | Function and route handler design rules are duplicated between STYLE_5 and STYLE_6; while both are slated to merge into `docs/code-style-guide.md` in Migration §2, the exact consolidation and removal of overlapping sections still requires manual editorial decisions in the Code Standards & Architecture phase. |
| ISS-0108 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 46 | **Overlap with api-patterns-guide**: The recommendations for thin handlers and service delegation overlap heavily with `docs/to_integrate/api-patterns-guide.md` (especially sections on routers and services). | Duplicates | CONFLICT | Code Standards & Architecture | STYLE_5’s thin-handler/service-delegation guidance overlaps heavily with `api-patterns-guide`, and both feed into `docs/api.md`/`docs/code-style-guide.md` per Migration §2; resolving which document owns which details and eliminating repetition remains a Phase 3 consolidation task. |
| ISS-0109 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 47 | **Overlap with fastapi-best-practices**: Async/await and concurrency guidance overlaps with `docs/to_integrate/fastapi-best-practices.md`; duplication should be reduced. | Duplicates | CONFLICT | Code Standards & Architecture | Async/await and concurrency guidance appears both in STYLE_5 and `fastapi-best-practices.md`; Migration §2 maps both into `docs/api.md` and `docs/code-style-guide.md`, but the overlapping FastAPI best-practices content must still be de-duplicated when drafting the canonical API/style docs. |
| ISS-0110 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 48 | **Missing complexity enforcement details**: Suggests small functions but does not specify how tools (for example, Ruff, pylint) enforce max complexity or lines; should be aligned with `pyproject.toml`. | Gaps | CONFLICT | Code Standards & Architecture | The STYLE_5 recommendation for small functions without specifying concrete complexity limits is only partially addressed by ADR-0005 and planned Ruff standardization in `pyproject.toml`; the gap between prose guidance and enforceable Ruff (or other tool) settings must be closed explicitly during Code Standards & Architecture migration work. |
| ISS-0111 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 49 | **Callback section relevance**: Emphasizes callback-style patterns that are less common in idiomatic Python FastAPI code; this section may be de-emphasized or simplified. | Staleness | CONFLICT | Code Standards & Architecture | Callback-heavy patterns in STYLE_5 are being merged into the canonical code-style-guide (Migration Matrix: STYLE_5 → docs/code-style-guide.md), but the plan does not explicitly decide whether to keep or de-emphasize callbacks, so this requires a manual content decision when consolidating routing patterns. |
| ISS-0112 | working/phase1/summaries/docs/to_integrate/STYLE_5.summary.md | 50 | **File naming vs router content**: Recommends one responsibility per file, but `metadata_router_v1.py` contains multiple route functions; expectations for router modules should be clarified. | Naming | CONFLICT | Code Standards & Architecture | The one-responsibility-per-file advice in STYLE_5 will be merged into docs/code-style-guide.md, but the migration plan does not resolve the tension with current router modules like app/routes/metadata_router_v1.py containing multiple handlers, so expectations for router file responsibilities must be clarified during Phase 3 architecture/style work. |

## Instructions

For each issue listed above:

- Open `docs/to_integrate/STYLE_5.md`.
- Confirm whether the described problem is still present.
- If it is still present, edit `docs/to_integrate/STYLE_5.md` to resolve it, aligning with current ADRs, `pyproject.toml`, and the consolidated `docs/code-style-guide.md`.
- If it has already been resolved, ensure the intent of the fix remains clear and that no contradictory guidance remains.

Focus only on the concerns described in these issues; do not introduce unrelated changes.
