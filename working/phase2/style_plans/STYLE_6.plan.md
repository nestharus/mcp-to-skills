# STYLE issues plan for docs/to_integrate/STYLE_6.md

This plan is generated from `issues_index.csv` and is scoped to `docs/to_integrate/STYLE_6.md`.

## Relevant issues

| id | doc | line_no | description_text | issue_type | classification | classification_ref | notes |
|---|---|---|---|---|---|---|---|
| ISS-0113 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 45 | **Major duplication with api-patterns-guide**: Large portions of STYLE_6 (project structure, routers, services, repositories, and DI) closely mirror `docs/to_integrate/api-patterns-guide.md`; these two documents should be merged rather than maintained separately. | Duplicates | CONFLICT | Code Standards & Architecture | Migration plan routes both STYLE_6 and api-patterns-guide into docs/code-style-guide.md and docs/api.md but does not spell out how to merge overlapping sections on project structure, routers, services, and DI; this duplication needs manual consolidation when creating the canonical API/style docs. |
| ISS-0114 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 46 | **Overlap with fastapi-best-practices**: Architectural and SOLID/clean-architecture advice overlaps with `docs/to_integrate/fastapi-best-practices.md`; content should be deduplicated or clearly scoped. | Duplicates | CONFLICT | Code Standards & Architecture | STYLE_6 and fastapi-best-practices are both mapped to docs/api.md and docs/code-style-guide.md, yet the plan only generally says to deduplicate overlaps; the concrete conflicts between SOLID/clean-architecture advice and fastapi-best-practices must be resolved during doc merge work. |
| ISS-0115 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 47 | **Aspirational vs descriptive architecture**: The guide describes a mature layered architecture that is not yet implemented here (no repositories, minimal DI, hardcoded responses in `metadata_router_v1.py`), so it currently functions as a target state rather than a description of the existing system. | Staleness | CONFLICT | Code Standards & Architecture | STYLE_6 describes a fully layered architecture (repositories, DI, non-hardcoded responses) that the current codebase does not yet implement; the migration plan positions architecture.md and code-style-guide.md as targets but leaves it to later phases to reconcile aspirational guidance with today’s simpler FastAPI app, so this mismatch requires deliberate adjustment instead of being auto-resolved. |
| ISS-0116 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 48 | **Database assumption**: Assumes a relational database and SQLAlchemy sessions, but this project has no concrete database setup; guidance should be made database-agnostic or clearly marked as future work. | Staleness | CONFLICT | Code Standards & Architecture | STYLE_6 assumes a relational DB and SQLAlchemy sessions while the project currently has no concrete DB layer; although architecture and style docs will centralize such guidance, the plan does not decide whether to make database guidance agnostic or explicitly future-oriented, so content authors must resolve this when drafting architecture.md and docs/api.md. |
| ISS-0117 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 49 | **Missing custom exception layer**: Recommends custom business exceptions (for example, `UserNotFoundError`, `UserAlreadyExistsError`) and a centralized error-handling strategy, but the project lacks an `app/core/errors.py` or equivalent exception hierarchy. | Gaps | RESOLVED | Migration §3 – app/core/errors.py (new) | Migration Matrix (Non-Documentation Files) explicitly calls for introducing app/core/errors.py with centralized error types and HTTP mapping, and references coordinating this with logging/error-handling guidance in docs/code-style-guide.md and docs/api.md, directly addressing the missing custom exception layer. |
| ISS-0118 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 50 | **Dependency injection not implemented end-to-end**: Shows complete DI chains (database → repository → service), while `app/core/dependencies.py` only wires settings; integrating services and repositories via DI remains future work. | Gaps | CONFLICT | Code Standards & Architecture | The plan notes that app/core/dependencies.py should align with DI patterns in docs/api.md and docs/architecture.md, but it does not yet define concrete end-to-end DI chains or implementation tasks for repositories/services; realizing the full DI chain remains future work that must be designed and implemented in later phases. |
| ISS-0119 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 51 | **Domain mismatch**: Examples reference generic User/Project domains instead of the MCP metadata domain (for example, `MetadataItem`, `FetchRequest`, and related contracts in `app/contracts/metadata_contract.py`). | Staleness | CONFLICT | Code Standards & Architecture | STYLE_6’s User/Project examples are scheduled to be merged into docs/architecture.md and docs/api.md, but the migration plan does not specify rewriting them into MCP-specific terms like MetadataItem and FetchRequest; aligning examples with the actual metadata domain will require manual editorial work when consolidating the docs. |
| ISS-0120 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 52 | **Overlap with STYLE_5**: Thin route handlers and service delegation rules are repeated in STYLE_5; these should be consolidated into a single set of routing/service guidelines. | Duplicates | CONFLICT | Code Standards & Architecture | Migration plan sends both STYLE_5 and STYLE_6 into the unified code-style-guide and API docs, and explicitly calls out resolving STYLE_* duplication red flags, but it does not provide specific consolidation rules for thin route handlers and service delegation; these overlapping routing/service guidelines must be merged and normalized during Phase 3 style doc work. |
| ISS-0121 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 53 | **Overlap with STYLE_1**: Type-hinting and docstring requirements repeat STYLE_1 guidance and should be centralized in a unified style guide. | Duplicates | RESOLVED | Migration §2 (STYLE_6 → docs/code-style-guide.md) | Type-hinting and docstring rules from STYLE_6 are explicitly slated to be merged into the single canonical docs/code-style-guide.md alongside other STYLE_* docs, which centralizes and deduplicates style guidance. |
| ISS-0122 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 54 | **OpenAPI tooling disconnect**: Discusses automatic OpenAPI generation but does not reference the actual tooling in this repo (`scripts/gen_openapi.py` and `openapi/openapi.json`), which should be aligned. | OpenAPI | RESOLVED | Migration §3 (scripts/gen_openapi.py & openapi/openapi.json alignment) | The migration plan’s non-doc matrix calls out scripts/gen_openapi.py and openapi/openapi.json as managed artifacts tied to ADR-0004 and the new docs/api.md, resolving the disconnect between OpenAPI guidance and the actual tooling. |
| ISS-0123 | working/phase1/summaries/docs/to_integrate/STYLE_6.summary.md | 55 | **Testing patterns vs current tests**: Mentions DI-based testing strategies and dependency overrides, but does not reference the existing testing setup (`tests/conftest.py`, `docs/TEST.md`, `docs/TESTING_ARCHITECTURE.md`), which already define how tests are structured. | Testing | RESOLVED | Migration §2 (TEST docs → docs/testing-guide.md) & §3 (tests/conftest.py alignment) | Testing strategies, including DI and dependency overrides, are being consolidated from STYLE_6, TEST.md, and TESTING_ARCHITECTURE.md into docs/testing-guide.md with corresponding updates to tests/conftest.py, aligning narrative guidance with the existing pytest setup. |

## Instructions

For each issue listed above:

- Open `docs/to_integrate/STYLE_6.md`.
- Confirm whether the described problem is still present.
- If it is still present, edit `docs/to_integrate/STYLE_6.md` to resolve it, aligning with current ADRs, `pyproject.toml`, and the consolidated `docs/code-style-guide.md`.
- If it has already been resolved, ensure the intent of the fix remains clear and that no contradictory guidance remains.

Focus only on the concerns described in these issues; do not introduce unrelated changes.
