# STYLE issues plan for docs/to_integrate/STYLE_8.md

This plan is generated from `issues_index.csv` and is scoped to `docs/to_integrate/STYLE_8.md`.

## Relevant issues

| id | doc | line_no | description_text | issue_type | classification | classification_ref | notes |
|---|---|---|---|---|---|---|---|
| ISS-0130 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 44 | **`mypy` not installed**: STYLE_8's strong emphasis on `mypy` (strict mode, hooks, CI) is out of sync with the current project, which does not include `mypy` as a dependency or mention it in any primary docs; a decision is needed on whether to adopt `mypy` or remove/soften this guidance. | Staleness | RESEARCH | Ruff Configuration | STYLE_8’s strong mypy emphasis conflicts with the current Ruff-only toolchain and ADR-0005; additional research is needed to decide whether to add mypy (and how to configure it alongside Ruff) or to revise style guidance to match the existing lint setup. |
| ISS-0131 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 45 | **Pre-commit configuration mismatch**: The recommended multi-repo pre-commit setup conflicts with the actual `.pre-commit-config.yaml`, which relies on a single `local` hook running `uv run lint`; documentation and configuration must be reconciled to avoid confusion. | Conflicts | CONFLICT | Code Standards & Architecture | The mismatch between the recommended multi-repo pre-commit setup and the actual single local `uv run lint` hook is called out generally in the migration plan (STYLE_7/STYLE_8 into `docs/code-style-guide.md` and `docs/workflow-and-ci.md`), but there is no specific ADR or migration step that explicitly reconciles the exact `.pre-commit-config.yaml` vs. docs guidance; this must be resolved during Code Standards & Architecture doc consolidation. |
| ISS-0132 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 46 | **Script section mismatch**: The use of `[tool.uv.scripts]` in examples does not match the project's current `[project.scripts]` usage; STYLE_8 should be updated to reflect how `uv` is actually invoked here. | Conflicts | CONFLICT | Code Standards & Architecture | The documented use of `[tool.uv.scripts]` vs. the actual `[project.scripts]` usage is a doc–config inconsistency not explicitly addressed in ADRs or the migration plan; it will need to be corrected while merging STYLE_8 into `docs/code-style-guide.md` and updating `pyproject.toml` patterns under the Code Standards & Architecture theme. |
| ISS-0133 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 47 | **Duplicate tooling documentation**: STYLE_8 substantially overlaps with `docs/to_integrate/linting-guide.md` and the `README.md` Code Quality section; Phase 2 integration should merge these into a single, authoritative description of the tooling stack. | Duplicates | RESOLVED | Migration §2 – STYLE_8 → docs/code-style-guide.md; docs/to_integrate/linting-guide.md → docs/workflow-and-ci.md; README.md → README.md; docs/README.md | The migration matrix explicitly merges STYLE_8 into `docs/code-style-guide.md`, moves linting-command workflow details into `docs/workflow-and-ci.md`, and refreshes `README.md`/`docs/README.md`, which together establish a single authoritative description of linting and tooling, resolving the duplication between STYLE_8, the linting guide, and README. |
| ISS-0134 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 48 | **Version drift**: Hard-coded tool versions in STYLE_8 do not match `pyproject.toml` and will quickly become outdated; examples should either be updated or rewritten to be version-agnostic. | Staleness | RESEARCH | Ruff Configuration | Hard-coded tool versions in STYLE_8 that diverge from `pyproject.toml` require deciding how to express version guidance (pinning vs. version-agnostic examples) and aligning with the standardized Ruff and tooling setup described in the migration plan; this depends on tooling/versioning best practices and should be treated as a Ruff/tooling configuration research task before updating the docs. |
| ISS-0135 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 49 | **Import ordering overlap**: Import ordering guidance duplicates content from STYLE_1 and other style docs; consolidated style documentation should avoid conflicting rules and pick a single source of truth. | Duplicates | RESOLVED | Migration §2 – STYLE_* → docs/code-style-guide.md (STYLE-IMPORTS red flag) | The migration plan explicitly calls out resolving STYLE import-ordering conflicts (e.g., `STYLE-IMPORTS` red flag) by consolidating all STYLE_* import rules into a single section in `docs/code-style-guide.md`, making that doc the single source of truth and eliminating overlapping import-order guidance from STYLE_8 and other style docs. |
| ISS-0136 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 50 | **Workflow inconsistency**: STYLE_8's narrative of separate `format` and `lint` commands differs from the reality that `uv run lint` (via `scripts/lint.py`) already executes formatting; documentation should clarify that a single command is the canonical workflow. | Conflicts | RESOLVED | Migration §2 – docs/to_integrate/linting-guide.md → docs/workflow-and-ci.md; README Code Quality | The migration plan normalizes lint and format commands around `uv run lint` (Ruff format then lint) in both `docs/workflow-and-ci.md` and the updated README, explicitly addressing the `WF-LINT-COMMANDS` red flag; this supersedes STYLE_8’s separate `format` vs. `lint` narrative and clarifies that `uv run lint` is the canonical combined workflow. |
| ISS-0137 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 51 | **Missing Ruff config details**: STYLE_8 mentions that Ruff is configured via `pyproject.toml` but does not reflect this repository's actual settings (e.g., 100-character line length); integrated docs should reference the concrete configuration. | Gaps | RESOLVED | Migration §3 – pyproject.toml & .pre-commit-config.yaml; STYLE_* → docs/code-style-guide.md | The migration plan mandates standardizing Ruff configuration in `pyproject.toml` (including line length) and aligning `.pre-commit-config.yaml` with ADR-0005, while consolidating style and tooling guidance into `docs/code-style-guide.md`; this ensures the integrated docs will point to and reflect the concrete Ruff config (e.g., 100-character lines), closing the missing-detail gap in STYLE_8. |
| ISS-0138 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 52 | **`TYPE_CHECKING` imports context**: While TYPE_CHECKING usage is shown, the rationale (avoiding import cycles and runtime costs) is only lightly touched on; integration work should ensure this guidance is connected to broader import and typing patterns from other style docs. | Gaps | CONFLICT | Code Standards & Architecture | The limited rationale around `TYPE_CHECKING` imports in STYLE_8 needs to be woven into the broader import and typing patterns across STYLE_* when merging into `docs/code-style-guide.md` under Code Standards & Architecture; the migration plan notes consolidation of type-hinting and import rules but does not yet specify how `TYPE_CHECKING` rationale will be expanded, so this remains a content gap to address during that consolidation. |
| ISS-0139 | working/phase1/summaries/docs/to_integrate/STYLE_8.summary.md | 53 | **CI integration specifics**: STYLE_8 vaguely recommends adding `uv run lint` to CI without showing concrete CI configuration; this must be coordinated with `docs/to_integrate/git-workflow.md` and the project's actual CI workflows. | Gaps | CONFLICT | Workflow, Releases, CI | STYLE_8’s vague `uv run lint` CI recommendation must be reconciled with the concrete CI workflows and branching guidance that will live in `docs/workflow-and-ci.md` and the actual `.github/workflows/*`; the migration plan flags `WF-LINT-COMMANDS` and CI standardization but leaves the exact CI snippet design to Phase 5, so this is an open workflow/CI integration detail to resolve. |

## Instructions

For each issue listed above:

- Open `docs/to_integrate/STYLE_8.md`.
- Confirm whether the described problem is still present.
- If it is still present, edit `docs/to_integrate/STYLE_8.md` to resolve it, aligning with current ADRs, `pyproject.toml`, and the consolidated `docs/code-style-guide.md`.
- If it has already been resolved, ensure the intent of the fix remains clear and that no contradictory guidance remains.

Focus only on the concerns described in these issues; do not introduce unrelated changes.
