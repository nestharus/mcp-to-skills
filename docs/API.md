# API Reference

## `GET /api/metadata/v1/health`

Returns the broker's health status.

## `GET /api/metadata/v1/sample`

Returns a sample `MetadataItem` using the contract defined in `app/contracts/metadata_contract.py`.
